import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  tag: {
    paddingHorizontal: 12,
    paddingVertical: 6,
    backgroundColor: "#eee",
    borderRadius: 16,
    marginRight: 8,
  },
  selectedTag: {
    backgroundColor: "#333",
  },
  tagText: {
    fontSize: 14,
    color: "#333",
  },
  selectedText: {
    color: "#fff",
  },
});
