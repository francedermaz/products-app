import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  screen:{
    marginTop: 12,
  },
  list: {
    paddingHorizontal: 16,
    paddingTop: 12,
  },
  skeleton: {
    paddingTop: 12,
  },
});
